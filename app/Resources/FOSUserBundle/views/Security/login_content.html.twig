{% trans_default_domain 'FOSUserBundle' %}
{#
    <div>
    <form class="m-t"  action="{{ path("fos_user_security_check") }}" method="POST">
        <div class="row">
            <div class="col-md-6">
                <div class="touch-left">
                    Nom d&apos;utilisateur ou e&dash;mail
                    <br>
                    <input type="text" id="username" name="_username" autocomplete="OFF" value="{{ last_username }}" required="required"  placeholder="Nom d'utilsateur" style="color:black;" />
                    <br>
                    <br>

                    Mot de passe
                    <br>
                    <input type="password" id="password" name="_password" required="required"  placeholder="Mot de passe" style="color:black;" />
                    
                </div>
            </div>
        </div>
        <div class="form_submit">
            <input type="submit" name="submit" value="Connexion"/>
        </div>
    </form>
</div>
#}

						{% if app.request.hasPreviousSession %}
						    {% for type, messages in app.session.flashbag.all() %}
						        {% for message in messages %}
						            <div class="flash-{{ type }}">
						                {{ message }}
						            </div>
						        {% endfor %}
						    {% endfor %}
						{% endif %}
<!-- cart page content section start -->
    <section class="login-page">
        <div class="container">
            <div class="row">
                <div class="col-sm-6">
                    <div class="row">
                        <div class="single-check">
                            <form class="m-t"  action="{{ path("fos_user_security_check") }}" method="POST">
                                <div class="single-input p-bottom50 clearfix">
                                    
                                    <div class="col-xs-12">
                                        <label>Email:</label>
                                        <div class="input-text">
                                            <input type="text" id="username" name="_username" autocomplete="OFF" value="{{ last_username }}" required="required"  placeholder="Nom d'utilsateur" style="color:black;" />
                                        </div>
                                    </div>
                                    <div class="col-xs-12">
                                        <label>Password:</label>
                                        <div class="input-text">
                                            <input type="password" id="password" name="_password" required="required"  placeholder="Mot de passe" style="color:black;" />
                                        </div>
                                    </div>
                                    <div class="col-xs-12">
                                        <div class="forget">
                                            <a href="#">Forget your password?</a>
                                        </div>
                                    </div>
                                    <div class="col-xs-12">
                                        <div class="submit-text">
                                            <input type="submit" name="submit" value="Login">
                                        </div>
                                    </div>
                                </div>
                            </form>
                        </div>
                    </div>
                </div>
                
            </div>
        </div>
    </section>



