{% extends "adminLayout.html.twig" %}

{% block stylesheets %}
    {{parent()}}

    <!-- FooTable -->
    <link href='{{asset("adminTemplate/css/plugins/footable/footable.core.css")}}' rel="stylesheet">

    <link href='{{asset("adminTemplate/css/animate.css")}}' rel="stylesheet">
    <link href='{{asset("adminTemplate/css/style.css")}}' rel="stylesheet">

{% endblock %}

{% block body %}

    <div class="row wrapper border-bottom white-bg page-heading">
            <div class="col-lg-10">
                <h2>Liste des utilisateurs</h2>
                <a href="{{ path('client_new') }}"><i class="fa fa-plus"></i>Ajout utilisateur</a>
            </div>
        </div>

        <div class="wrapper wrapper-content animated fadeInRight ecommerce">

            <div class="row">
                <div class="col-lg-12">
                    <div class="ibox">
                        <div class="ibox-content">

                             <table class="footable table table-stripped toggle-arrow-tiny" data-page-size="15">
                                <thead>
                                <tr>

                                    <th data-toggle="true">Nom</th>
                                    <th data-toggle="true">Prenom</th>
                                    <th data-toggle="true">Adresse</th>
                                    <th data-toggle="true">Numero Telephone</th>
                                    <th data-toggle="true">Nom d'utilisateur</th>
                                    <th data-toggle="true">E-mail</th>
                                    <th data-toggle="true">Droit</th>
                                    
                                    <th class="text-right" data-sort-ignore="true">Action</th>

                                </tr>
                                </thead>
                                <tbody>

                                {% for user in users %}

                                    <tr>
                                    
                                    <td>
                                        {{ user.username }}
                                    </td>
                                   
                                    <td class="text-right">
                                        <div class="btn-group">
                                            <a href="{{ path('user_show',{id : user.id}) }}"><button class="btn-white btn btn-xs">Voir</button></a>
                                            <a href="{{ path('user_edit',{id : user.id}) }}"><button class="btn-white btn btn-xs">Modifier</button></a>
                                            <a href="{{ path('user_delete',{id : user.id}) }}"><button class="btn-white btn btn-xs">Supprimer</button></a>
                                            
                                        </div>
                                    </td>
                                </tr>
                                {% endfor %}
                                
                                </tbody>
                                <tfoot>
                                <tr>
                                    <td colspan="6">
                                        <ul class="pagination pull-right"></ul>
                                    </td>
                                </tr>
                                </tfoot>
                            </table>


                        </div>
                    </div>
                </div>
            </div>


        </div>






{% endblock %}

{% block javascripts %}
    {{parent()}}

<script src='{{asset("adminTemplate/js/plugins/footable/footable.all.min.js")}}'></script>

<!-- Page-Level Scripts -->
<script>
$(document).ready(function() {

    $('.footable').footable();

});

</script>



{% endblock %}