
<!DOCTYPE html>
<html>
<head>
	<title></title>
	<style type="text/css">
		body {
		    background: grey;
		    margin-top: 120px;
		    margin-bottom: 120px;
		}

        table, th, td {
            border: 1px solid black;
        }

        table
        {
            border-collapse : collapse;
            width: 100%;
        }

        th {
            height: 50px;
        }

        th,td {
            text-align: center;
            width: 20%;
            border: 1px solid black;
            color: black;
        }

        .total{
            text-align: left;
            font-weight: bold;
        }
	</style>
</head>
<body>
<div>
    <h1>{{ proforma.client.prenom }}, votre commande a &eacute;t&eacute; enregistr&eacute;e avec succes !</h1>
    <h1> Les petits bras vous remercie!</h1>    
    <hr>
    <table>
        <thead>
            <tr>
                <th>N&deg;</th>
                <th>Article</th>
                <th>P.U</th>
                <th>Quantit&eacute;</th>
                <th>Total</th>
            </tr>
        </thead>
        <tbody>
            {% set i = 1   %}
            {% for order in proforma.lpCollection|reverse %}
                <tr>
                    <td> {{ i }} </td>
                    <td> <h6>{{ order.article.designation }}</h6> </td>
                    <td> <h6>{{ order.prix|number_format(0, '.', ' ') }}</h6> </td>
                    <td> <h6>{{ order.qte }}</h6> </td>
                    <td> <h6>{{ (order.prix * order.qte)|number_format(0, '.', ' ') }}</h6>   </td>
                    {% set i = i +1   %}
                </tr>
            {% endfor %}
                <tr>
                    <td colspan="4" class="total"><h5><b>Total</b></h5></td>
                    <td><h5><b>{{ proforma.total|number_format(0, '.', ' ') }} FCFA</b></h5></td>									
                </tr>
        </tbody>
    </table>
    <div style="width: 100%;  text-align: center;">
        
        <a href="{{ absolute_url(path("shop")) }}" style="margin-top: 50px; color: #188fff;"><h4>Aller à la boutique</h4></a>
    </div>
</div>
</body>
</html>